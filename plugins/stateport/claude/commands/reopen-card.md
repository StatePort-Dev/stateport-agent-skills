---
description: Reopen the exact StatePort Card and revision against the intended current code.
disable-model-invocation: true
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/stateport-debugging/SKILL.md` and the relevant
bundled references, then reopen the exact Card and revision supplied here or
retained in the approved handoff: `$ARGUMENTS`. Follow the canonical reuse
branch and report which current code and run were actually observed.
