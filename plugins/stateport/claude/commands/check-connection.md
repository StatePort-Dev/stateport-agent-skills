---
description: Check whether the local StatePort MCP connection exposes the required public capabilities.
disable-model-invocation: true
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/stateport-debugging/SKILL.md` and its public
MCP operation map. For this request, `$ARGUMENTS`, inspect the connected
server's actual public discovery and schemas. Report whether existing-Card
operations and agent Capture are available separately; do not change MCP
configuration or runtime permissions automatically.
