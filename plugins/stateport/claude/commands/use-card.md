---
description: Investigate a browser bug using a supplied StatePort Card and revision.
disable-model-invocation: true
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/stateport-debugging/SKILL.md` and the relevant
bundled references, then apply its supplied-Card branch to this request:
`$ARGUMENTS`. If the exact Card and revision are missing, request them before
opening a Card. Use only discovered public StatePort MCP capabilities.
