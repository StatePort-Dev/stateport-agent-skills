{
  "name": "@stateport-dev/agent-skills",
  "version": "0.1.0-beta.2",
  "private": true,
  "description": "Canonical StatePort agent skills and provider integration sources.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "sync": "node scripts/generate-codex-plugin.mjs",
    "check": "node scripts/check.mjs",
    "verify": "npm test && npm run check"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/StatePort-Dev/stateport-agent-skills.git"
  }
}
